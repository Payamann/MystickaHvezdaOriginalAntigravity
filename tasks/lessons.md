# Lessons Learned

| Pattern | Correction | Date |
|---------|------------|------|
| [near-miss] Změna marketingové copy ve statickém HTML se v prohlížeči neprojevila — cenik.js přepisuje texty karet přes textContent po načtení | Při změně copy v HTML vždy grepnout js/ na textContent/innerHTML přepisy téhož elementu a změnit obě místa | 2026-07-03 |
| [env] Jest ve worktree nenajde testy — testMatch glob se rozbije na `\.claude` v cestě | Spouštět s explicitním `--testMatch "**/server/tests/**/*.test.js" --testMatch "**/tests/frontend/**/*.test.js"` | 2026-07-03 |
| [env] Stránky načítají js/dist/* bundly, ne js/* zdrojáky | Po každé změně v js/ spustit `npm run build:js` a bumpnout `?v=` u dotčeného script tagu | 2026-07-03 |
| [near-miss] Slovo `export`/`import` KDEKOLIV ve zdroji js/ (i v komentáři nebo řetězci jako "Blob export fallback") přepne build-js.mjs na ES modul a static-html-csp test pak vyžaduje type="module" | V classic (IIFE) JS souborech se vyhnout samostatným slovům export/import i v textech; použít render/build/conversion | 2026-07-04 |
| [incident] Deploy-poller fetchoval novou verzovanou URL (?v=10) PŘED dokončením deploye → Cloudflare zacachoval starý obsah pod novou URL a servíroval ho i po deployi | Deploy ověřovat přes HTML marker nebo s náhodným cache-busterem (&cb=...); přesnou verzovanou asset URL fetchnout až JEDNOU po potvrzení HTML. Pozn.: esbuild eskapuje ne-ASCII (Sdílet→Sd\xEDlet), grep podle českých řetězců v dist nefunguje | 2026-07-04 |
| [near-miss] `server/tests/setup.mjs` nastavuje `RESEND_API_KEY='test-resend-key'` (fake, ale NENÍ prázdný) → jakýkoliv test, co projde až k `email-service.js` send funkci, udělá skutečný síťový request na Resend API (pomalé, 15-30s, závislé na síti) místo bezpečného no-opu. Existující testy (email-queue, personal-map-pdf, horoscope-pdf) tomu záměrně předchází — testují jen kolem send kroku, nikdy skrz něj | Nový job/test co potřebuje simulovat úspěšné/neúspěšné doručení: injectovat volatelnou funkci (default = reálná implementace) místo mockování modulu — stejný vzor jako `fetchImpl` v `services/alerts.js`. Nikdy nespoléhat na to, že fake API klíč v test env bezpečně no-opuje | 2026-07-06 |
| [near-miss] Chtěl jsem spustit reálný `node server/index.js` z worktree jen kvůli vizuální kontrole nové admin.html sekce — auto mode classifier to zastavil, protože `initializeOneTimeOrderReconciliationJob()` běží hned při startu a bez `.env` nelze zaručit, že se nenatáhnou reálné Supabase/Resend credentials odjinud (živé mutace produkčních dat / reálné e-maily) | Pro UI ověřující kód, co má side-effecty na startu (cron joby, webhooky), NEZAPÍNAT reálný server jen kvůli vizuální kontrole — ověřit přes supertest+real app v jestu (mock Supabase, žádné reálné síťové efekty) misto browser preview | 2026-07-06 |
| [incident] Investovali jsme 330 unikátních odstavců + H1 + newsletter do kompatibilita/*.html (66 souborů) — po deployi se ukázalo, že server tyto stránky 301-redirectuje na partnerska-shoda (regex route v server/index.js + CZECH_COMPATIBILITY_SLUGS) a soubory se v produkci NIKDY neservírují. Statický preview server v worktree redirecty obcházel, takže vizuální ověření prošlo | Před investicí do obsahu jakéhokoliv clusteru NEJDŘÍV curl na produkční URL (sledovat 301/noindex/canonical) nebo grep server/index.js na redirect mapy a regex routes pro daný adresář. Statický preview NEnahrazuje ověření skutečného servírování — express routing je součást produktu | 2026-07-06 |
| [incident] Ceník měl mobilní CLS ~0.24 (POOR). Dlouho jsem chybně obviňoval font swap / JS přepisy textu / vkládaný recommendation banner a marně přidával pre-render + guardy. Pravá příčina: `body.page-pricing.cookie-banner-active` v cenik.css zmenšovalo celý hero (font-size titulku 41→31px, display:none trust-badge, margin-top 12.5rem) a aplikovalo se až KDYŽ se cookie lišta objevila (~1,8 s) → reflow celého hero | CLS diagnostika: NEHÁDAT příčinu. Měřit (a) `PerformanceObserver` layout-shift `sources` s `previousRect→currentRect`, (b) computed `font-size`/`getBoundingClientRect` klíčových prvků v čase (400ms vs 3000ms), (c) cold vs warm-cache (izoluje font timing). Pozdě přidaná třída na body/html (cookie banner, dark mode) + rozdílné rozměry = klasický CLS; fix = neaplikovat rozměrové změny pozdě, nebo přidat třídu před first paint. Fonty: `font-display:optional` swap nezaručeně nepotlačí při relayoutu — preload above-the-fold fontů | 2026-07-14 |
| [finding] Freemium limit křišťálové koule se serverově vynucoval jen pro přihlášené free (`if (!req.isPremium && req.user?.id)`), anonymní byli omezeni jen 20/hod IP limiterem → přihlášení = víc omezení než anonymita (zvrácený funnel) a byznys pravidlo „3/den" pro anonymní neplatilo | Freemium/limit gate VŽDY vynucovat i pro anonymní (server-authoritative httpOnly cookie / IP), ne jen pro `req.user?.id`; klientský localStorage limit je jen kosmetika, ne vynucení. Při auditu limitů grepnout `req.user?.id` u gate podmínek | 2026-07-13 |
| [near-miss] Doporučil jsem „share-image loop wave 2" jako otevřenou příležitost podle status sekce app-improvement-plan (2026-07-04) — grep ukázal, že všechny 4 nástroje už integraci mají včetně dist buildů; status sekce plánovacích dokumentů stárnou během dní | Před zařazením „unbuilt" položky z plánovacího dokumentu do doporučení VŽDY grepnout kód (identifikátor funkce/eventu), jestli už není hotová | 2026-07-12 |
| [incident] Placené produkty nikdy nefungovaly: (a) `payment_events` tabulka je PRÁZDNÁ za celou historii → Stripe webhooky nikdy nedorazily (endpoint na Dashboardu chybí/špatná URL/vypnutý po selháních); předplatné se aktivuje jen záchrannou success-page cestou (js/profile/dashboard.js posílá session_id), jednorázová PDF jsou čistě webhook-driven → nedoručí se. (b) Playwright Chromium chyběl na Railway. (c) reconciliation job fulfilloval podle status='checkout_created', což vzniká PŘED platbou → opuštěný košík dostane PDF zdarma | Při diagnostice plateb VŽDY zkontrolovat živý stav přes Stripe API (`checkout.sessions.list`) i webhook-log tabulku, ne jen funnel_events (ty mají strop a měří klientsky). Kód opraven (verify přes Stripe před fulfillmentem). Local .env je TEST klíč, produkce běží LIVE — Stripe API dotazy z local vidí TEST data, ne produkční. **⚠️ OPRAVA 2026-07-29: závěr „root-cause je Dashboard config" byl chybný** — endpoint je nastavený správně na `/webhook/stripe`; celá hypotéza stála na URL `/api/payment/webhook`, kterou chybně uváděl WEB-KNOWLEDGE a která v kódu nikdy neexistovala. Viz řádek níže | 2026-07-07 |
| [incident] Dva týdny jsem diagnostikoval platby proti **neexistující URL webhooku**. `docs/WEB-KNOWLEDGE.md` uváděl `/api/payment/webhook`, skutečná cesta je `POST /webhook/stripe` (mimo `/api`, kvůli raw body pro ověření podpisu). Z toho vzešel závěr „webhooky nechodí, protože endpoint na Stripe Dashboardu je špatně nakonfigurovaný" — a ten se dostal do WEB-KNOWLEDGE §8, VALUE-ANALYSIS (jako priorita č. 1 se skóre 125) i do doporučení uživateli. Screenshot Dashboardu od uživatele ukázal, že endpoint je celou dobu správně | **Cestu/název ověř grepem dřív, než na ní postavíš diagnózu** — `grep -rn "webhook" server/index.js` stálo 5 sekund, chybná domněnka stála dvě kola práce a jednu špatnou prioritizaci. Obecně: interní dokumentace **není zdroj pravdy o kódu**, je to poznámka, která mohla zestárnout nebo být od začátku špatně. Když z dokumentu přebíráš konkrétní identifikátor (cesta, název eventu, env proměnná), ověř ho v kódu. A když fakt neznáš, napiš do dokumentu „neověřeno" — odhad, který vypadá jako zjištění, je horší než mezera | 2026-07-29 |
| [incident] Oprava umístění tlačítka „Sdílet výsledek" (`ed30f8af`, 2026-07-19) se dvanáct dní nedostala k žádnému vracejícímu se návštěvníkovi. `js/dist/*` se servíruje s `Cache-Control: max-age=31536000, immutable`, ale všech 17 stránek načítalo `share-result.js` **bez `?v=`** — prohlížeč tedy rok drží starou kopii a ani nerevaliduje. Deploy proběhl, testy prošly, soubor na serveru byl nový, a přesto uživatel viděl původní chování. Širší rozsah: `analytics-init.js` je bez verze na 905 stránkách, `main.js` na 60 (a zároveň s verzí na 849 → dva nezávislé cache záznamy téhož souboru) | „Nasazeno" ≠ „doručeno". Po změně JS ověřit, že **cílové stránky** soubor načítají s `?v=` a parametr bumpnout — samotný build a deploy nestačí. Audit: `grep -rho 'js/dist/[a-z0-9./-]*\.js"' --include=*.html .` vypíše odkazy bez parametru. Pozor i na to, že service worker tohle nezachrání — `share-result.js` v jeho precache vůbec není, takže `CACHE_NAME` bump nic neřešil | 2026-07-31 |

## Golden Rules
1. **Never use `var`**: Always use `const` or `let`.
2. **Vanilla First**: No external libraries unless absolutely necessary.
3. **Responsive by Design**: All UI changes must be tested on mobile widths.
4. **Zero Placeholder Policy**: All images and text must be real/generated, never "lorem ipsum".
